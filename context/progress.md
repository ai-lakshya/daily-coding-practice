# Progress Log
| date | type | id | title | time_spent | self_rating | key_takeaway |
|------|------|----|-------|-----------:|:------------:|--------------|
| 2026-08-04 | dsa | lc150-0051 | Minimum Number of Arrows to Burst Balloons | | 3 | Interval point cover pattern (sort by end, sweep with one running boundary); solved correctly in one pass with a valid but less standard descending-start + fixed-anchor greedy |
| 2026-08-04 | concept | dsa-01 | Amortized analysis | | | Total cost over a worst-case sequence divided by n, not per-call worst case; dynamic array doubling gives O(1) amortized push_back despite occasional O(n) resizes because resizes happen at geometrically-spaced sizes |
| 2026-08-04 | concept | dsa-01 | Amortized analysis (follow-up) | | | Contrasted amortized (deterministic, sequence-based, no bad case) vs average-case (probabilistic, e.g. quicksort O(n^2) worst / O(n log n) average) analysis; worked all three amortized proof methods (aggregate, accounting $3-charge, potential Phi=2*size-capacity) on array doubling, all converging on the same constant, plus a second potential-method example (binary counter, Phi=1-bit count) |
| 2026-08-04 | dsa | lc150-0028 | Container With Most Water | | 4 | Two Pointers (inward-moving); correctly deduced greedy choice to discard shorter line |
| 2026-08-21 | sysdesign | sd-001 | Transaction Isolation Levels | | pending | Corrected "isolation = serializability" (that's only the top level) and "MVCC = Consistency Control"; full anomaly taxonomy (P0-P4/A5A/A5B), locking (strict 2PL) vs MVCC as competing implementation families for the same standard's permission table, snapshot-timing distinction between read committed/repeatable read, write skew survives snapshot isolation (H5/hospital on-call worked example), reaching real serializability via SSI or actual serial execution, PostgreSQL/MySQL/Oracle deviations from the standard. Cross-questioning this session went into transaction/ACID foundations instead of isolation specifics (see sd-002) — isolation-specific follow-ups still open for next session |
| 2026-08-21 | sysdesign | sd-002 | Transactions & ACID Properties | | pending | Transaction classification along four axes (autocommit vs explicit, read-only vs read-write, flat vs savepoint, local vs 2PC vs saga), PostgreSQL-vs-MySQL DDL transactionality gotcha; ACID walked with concrete examples (bank-transfer atomicity via WAL, CHECK-constraint consistency vs. the undeclared-invariant hospital on-call gap, isolation re-anchored to sd-001, durability via WAL+fsync and synchronous_commit=off); fsync mechanics (page cache vs. disk, device write-cache flush caveat); corrected an in-chat estimate against PostgreSQL docs — synchronous_commit=off's crash-loss window is 3x wal_writer_delay (~600ms at the 200ms default), not ~200ms flat |
